# REQ-028

Demand: DEM-002
Priority: must

## Statement

A entrega inclui testes de componente por comportamento para cada tela e um E2E Playwright da jornada completa de assinatura contra o stack real com API + Postgres + MinIO + SMTP de teste

## Acceptance

- Cada pagina tem teste cobrindo rota / primeira carga / dados exibidos / estado vazio / falha da API / mutacao com sucesso / mutacao com falha
- Existe E2E cobrindo registro de rubrica -> upload -> marcacao da area -> e-mail recebido -> assinatura com senha -> etapa visivel na linha do tempo
- O E2E roda contra servicos reais e nao contra a fronteira HTTP simulada
- Nenhuma evidencia de smoke e produzida com a API simulada

## Validation

Method: automated-test

Required evidence:

- Relatorio da suite de componentes + relatorio Playwright da jornada de assinatura contra o stack real
